== Building and running

This project is built with Ruby v.2.0.

To run locally you need to:
- check out the project source
- make sure you have the correct version of Ruby (2.0 or higher)
- from the project folder, issue `bin/bundle install` command
- from the project folder, issue `bin/rails s` command
- visit `http://localhost:3000` in your browser